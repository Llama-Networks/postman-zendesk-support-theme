{
  "name": "postman-zendesk-support-theme",
  "version": "1.15.15",
  "author": "Brandon Castillo",
  "private": true,
  "scripts": {
    "dev": "zat theme preview",
    "prettier:write": "prettier --write \"./**/*.{js,json,css,hbs}\"",
    "prettier:verify": "prettier --check \"./**/*.{js,json,css,hbs}\"",
    "nvmrc": "echo $(node -p -e 'require(\"./package\").engines.node.split(\">=\").join(\"\")') > .nvmrc"
  },
  "devDependencies": {
    "prettier": "3.2.5"
  },
  "engines": {
    "node": "20.11.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Llama-Networks/postman-zendesk-support-theme"
  }
}
